Place the SQLite JDBC driver JAR here as:

sqlite-jdbc.jar

On Windows, `setup_driver.bat` can download the pinned driver automatically.
The source code uses the standard JDBC API; the external JAR is only needed at compile/run time for SQLite access.
